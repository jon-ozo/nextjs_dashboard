import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
	/* config options here */
	// ppr: 'incremental', // partial prerendering is experimental and shouldn't be used in production
};

export default nextConfig;
